import { ForminsertPipe } from './forminsert.pipe';

describe('ForminsertPipe', () => {
  it('create an instance', () => {
    const pipe = new ForminsertPipe();
    expect(pipe).toBeTruthy();
  });
});
